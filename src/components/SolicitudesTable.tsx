// Componente no utilizado actualmente en el enrutado.
// Conservado como placeholder para evitar imports huérfanos en builds futuros.
const SolicitudesTable = () => null;
export default SolicitudesTable;
